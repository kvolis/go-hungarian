# go-hungarian

Hungarian algorithm implementation in Go with generics support.

## Features

- Generic numeric types (`int`, `int64`, `float64`, etc.)
- Minimization and maximization modes
- O(n³) time complexity
- Pure Go, no external dependencies

## Installation

```bash
go get github.com/kvolis/go-hungarian